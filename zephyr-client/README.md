# Zephyr Client App

Uses modern React, Typescript, and Vite. Optimised for performance.

To run a dev server, `npm run dev`

To build for production, `npm run build`, outputs will be in the `dist` folder
